{{!< default}} {{!-- Insert everything in this file into the {body} of the default.hbs template --}} {{#if
    @page.show_title_and_feature_image}} <body class="page-show-title">
    {{else}}

    <body>
        {{/if}}

        {{#post}}
        {{> "header"}}
        <main class="sm-content-grid sm-post-content {{post_class}} {{slug}}" id="sm-main">
            {{content}}
        </main>
        {{/post}}

    </body>